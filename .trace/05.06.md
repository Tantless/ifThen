# 2026/05/06

## 1. 创建合成真实性测试集重审任务
为 `tests/fixtures/realism_synthetic` 三段 AI 生成聊天记录创建 brainstorm 任务目录 `.trellis/tasks/05-06-05-06-synthetic-realism-fixture-review`，用于记录本次对时间连贯性、逻辑连贯性、真实聊天质感和项目可修改价值的审阅结论。

## 2. 完成三段合成聊天记录真实性审阅
审阅 `case-01-hidden-trauma-confession`、`case-02-conflict-repair`、`case-03-missed-window` 的 `conversation.txt`、`timeline.md`、`rewrite-points.md`、`truth-after-cutoff.md` 和 `generation-notes.md`，并将发现写入任务 PRD。确认主要问题包括时间分布过于规律、关键证据过度显性、隐藏事实边界被提前泄漏、对话修复过于教科书化，以及现有 fixture 测试未覆盖导出时间、时间语义、未来证据边界和重复线索阈值。

## 3. 启动语料拟真校验实现
根据用户确认的 validation-first 方向，将 `.trellis/tasks/05-06-05-06-synthetic-realism-fixture-review/task.json` 切换为实现阶段，并新增 `PRDS/PRD016-语料拟真校验.md` 绑定终版需求与后续交付记录。本阶段目标是在不重写三段完整聊天正文的前提下，先把导出时间、时间语义、未来证据边界和重复线索问题转成可执行校验。

## 4. 建立合成语料真实性校验入口
新增 `scripts/validate_realism_synthetic_corpus.py`，为 `tests/fixtures/realism_synthetic` 输出结构化 audit 报告，检查导出时间早于末条消息、强时段词冲突、future evidence 提前出现和重复线索过度显性。当前三段语料保留 3 个显式 waiver：case-01 `别对我太好` 重复过多、case-02 家庭检查 future evidence 提前出现、case-03 baseline 绑定的 19:23 `快睡` 句；同时修正 case-01 导出时间和 case-02 晚间“今天先撑过上午”的低风险正文错误，并在 `scripts/generate_realism_synthetic_corpus.py` 中补充强时段词约束和安全导出时间渲染。

## 5. 完成语料拟真校验收口验证
更新 `tests/test_realism_synthetic_fixtures.py` 将合成语料 audit 接入 pytest，要求当前没有未豁免 finding 且 waived key 精确匹配 3 个已知债务，并补充临时 fixture 回归验证未豁免导出时间和时间语义错误会导致报告失败。同步更新 `docs/realism-quality-rollout.md`、`plan/realism-08-quality-and-rollout.md`、`.trellis/spec/backend/index.md`、`.trellis/spec/backend/quality.md`、`.trellis/tasks/05-06-05-06-synthetic-realism-fixture-review/prd.md` 和 `PRDS/PRD016-语料拟真校验.md`，记录 validator 合同、执行方式和完成情况；验证 `PYTHONPATH=src python scripts/validate_realism_synthetic_corpus.py --output-json .pytest-tmp/realism-synthetic-validation.json` 通过，定向 synthetic/baseline 测试 8 项通过，`PYTHONPATH=src pytest -q` 133 项通过。

## 6. 创建原历史话题事实分支回复 brainstorm 任务
为“改写分支回复需要知道原时间线同一话题中对方真实状态”的需求创建 `.trellis/tasks/05-06-branch-original-topic-facts`，并写入 PRD 初稿。任务记录了用户提出的 `A: 在干嘛`、`B: 在看动漫`、改写为 `A: 在看动漫吗` 后理想回复 `B: 是的，你怎么知道？` 的目标场景，后续审阅现有 context、retrieval、simulation 和 branch session 代码后收敛技术方案。

## 7. 收敛原历史当下事实分支回复方案
审阅 `src/if_then_mvp/context_builder.py`、`src/if_then_mvp/retrieval.py`、`src/if_then_mvp/simulation.py`、`src/if_then_mvp/branch_sessions.py`、`src/if_then_mvp/worker.py` 及相关测试后，更新 `.trellis/tasks/05-06-branch-original-topic-facts/prd.md`。确认当前 `session_memory_pack` 只有 cutoff-safe、future evidence 和 branch facts 三层，缺少可表达“other 在目标时刻真实活动/私有状态”的中间事实层；PRD 中补充了三种方案，并推荐新增 `objective_moment_facts` 层，通过近目标原时间线窗口提供可影响 other 私有状态但不得泄漏来源的客观当下事实。

## 8. 记录原历史背景事实产品决策
根据用户确认，将 `.trellis/tasks/05-06-branch-original-topic-facts/prd.md` 中的 `objective_moment_facts` 语义收敛为“人物彼时现实背景描述”。该层用于让 LLM 知道 `other` 在目标时刻正在做什么、正在想什么、处于什么现实状态；这些内容可以由目标附近后文客观事实推得，并可影响 `other` 的私有状态和即时回复，但不能作为原时间线、后文证据或聊天来源在台词中暴露。

## 9. 细化背景事实不得直出台词边界
根据用户补充，将 `.trellis/tasks/05-06-branch-original-topic-facts/prd.md` 中的背景事实使用边界进一步收窄：`objective_moment_facts` 只能帮助 LLM 理解 `other` 的处境、注意力、情绪底色和反应合理性，不能作为台词直接素材被复述、引用或解释来源；若用户当前 self 输入没有自然触发该背景，LLM 不应主动把背景说出来。

## 10. 启动背景事实上下文实现
根据用户确认的实现方向，新增 `PRDS/PRD017-背景事实.md` 绑定终版需求与交付记录，并将 `.trellis/tasks/05-06-branch-original-topic-facts/task.json` 切换到 backend 实现阶段。本阶段实现范围限定为新增 `objective_moment_facts` 上下文层、接入实时分支 memory pack 与 prompt 护栏，并补充后端回归测试。

## 11. 完成背景事实上下文实现
新增 `src/if_then_mvp/context_builder.py` 的 `objective_moment_facts` 提取逻辑，从目标消息后的近目标原时间线窗口提取 `other` 的现实活动或注意力/情绪背景，并在 `src/if_then_mvp/retrieval.py` 中纳入 context pack、evidence policy、retrieval trace 和 retrieval budget。更新 `src/if_then_mvp/branch_sessions.py` 将该层持久化到 `session_memory_pack.layered_context_pack`，同时保持 cutoff-safe compatibility pack 不包含该层；更新 `src/if_then_mvp/simulation.py` 的 realtime branch reply prompt，明确背景事实只用于理解处境，不得被复述、引用或解释来源。

## 12. 完成背景事实回归验证
更新 `tests/test_branch_sessions.py`，新增“在看动漫吗”实时分支回归，通过 API 创建 branch session、worker 生成 reply prompt，并断言 prompt 包含 `other 此刻在看动漫` 背景事实及不得直出台词的护栏；同时保留 future evidence modeler-only 泄漏护栏断言。更新 `tests/test_retrieval.py` 固定新增 context pack trace、budget 和默认 payload 合同；验证 `PYTHONPATH=src python -m pytest -q` 通过，134 项测试通过。

## 13. 同步背景事实 rollout 合同
更新 `docs/realism-quality-rollout.md`，将 context pack 质量门槛从 `cutoff_safe_facts`、`future_evidence_digests`、`branch_facts` 三层扩展为包含 `objective_moment_facts` 的四层合同，并补充 realtime branch prompt 对该层“仅作背景参考、不得直出台词”的使用边界与 2026-05-06 本地 134 项 pytest 验证结果。
