## 1. 聊天记录定位断层问题排查
确认仓库当前状态并阅读 `.trace/SUMMARY.md`、Trellis 前端规范、React 常见陷阱与实现前检查清单。定位聊天窗口默认尾部加载、顶部继续加载更早消息、聊天记录弹窗定位消息三条链路，确认 `desktop/src/App.tsx` 在历史定位后把目标上下文与当前尾部窗口直接按 message id 合并排序，导致中间缺失消息未被建模却被渲染成连续列表。

## 2. 聊天窗口分页状态修复
修改 `desktop/src/App.tsx` 的消息分页状态，为会话窗口补充 `hasNewer` 与 `loadingNewer` 两个方向状态；默认尾部加载、导入初始化、失败回退都同步初始化双向分页。将历史定位逻辑改为直接切换到目标消息附近的连续上下文窗口，不再把该窗口与原尾部窗口强行合并；同时根据当前已知尾部窗口判断是否还存在向下补齐的后续消息。新增 `handleLoadNewerMessages()`，通过 `after` 游标按升序继续加载定位窗口后方的缺失消息。

## 3. 滚动补齐与回归验证
修改 `desktop/src/frontui/ChatWindow.tsx`，把消息区滚动逻辑从仅支持顶部加载更早消息扩展为同时支持底部加载后续消息，并分别维护顶部/底部 rearm、视口锚点和加载提示文案，避免向下补消息后自动跳到底部。更新 `desktop/tests/frontUiShell.test.tsx` 以匹配新的加载提示；在 `desktop/tests/visualShell.test.tsx` 新增“历史定位后向下滚动补齐中间消息”回归用例。执行 `npm run typecheck` 与 `npm test -- --run desktop/tests/frontUiShell.test.tsx desktop/tests/visualShell.test.tsx`，共 46 项测试通过。

## 4. 头像生图脚本最小实现
阅读 `desktop/public/avatars/image_model.env`、Trellis workflow 与共享 git 规范，确认当前任务只需在头像素材目录下新增最小脚本，不触碰现有前端逻辑。新建 `PRDS/PRD015-头像生图脚本.md`，收敛需求为“读取本地 env、使用 Responses API、支持按行批量 prompt、落盘到独立输出目录并做真实 smoke test”。

## 5. Responses 批量生图脚本与真实请求验证
新增 `desktop/public/avatars/generate_avatars_responses.py` 与 `desktop/public/avatars/avatar_prompts.txt`。脚本使用 `openai` Python SDK 读取 `image_model.env`，通过 `client.responses.create()` + `image_generation` tool 生成图片，解析 `image_generation_call.result` 的 base64 并写入 `generated/` 目录；同时把 `RESPONSE_MODEL_NAME` 和 `IMAGE_MODEL_NAME` 分离，避免把图像模型误填到 Responses 顶层 `model`。执行 `python generate_avatars_responses.py` 前的 SDK smoke test 与原始 HTTP 探测，当前 provider 均返回 `403 / Your request was blocked.`，已确认失败点位于上游网关拦截而非本地脚本语法。

## 6. 错误提示收口
调整 `desktop/public/avatars/generate_avatars_responses.py` 的入口错误处理，将 `OpenAIError` 与本地运行时错误统一收口为简短失败提示，避免后续批量运行时直接输出长 traceback，保留最小脚本结构不变。

## 4. 加载中继续滚动导致视口丢失修复
继续修复 `desktop/src/frontui/ChatWindow.tsx` 的视口冻结问题。确认旧实现只在触发加载的瞬间记录一次锚点；若用户在请求返回前继续滚动，消息插入后仍会按照旧锚点回弹，导致当前正在看的消息丢失。新增加载挂起期间的锚点刷新逻辑：只要 older/newer 分页仍在 pending，每次滚动都会用当前首个可见消息更新冻结锚点，等消息真正插入时再按最新可见位置恢复视口。同步为 `desktop/tests/frontUiShell.test.tsx` 新增“加载更早消息挂起时继续滚动仍保持最新可见位置”回归测试。再次执行 `npm run typecheck` 与定向 `npm test`，共 47 项测试通过。
