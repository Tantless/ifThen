## 1. 聊天记录定位断层问题排查
确认仓库当前状态并阅读 `.trace/SUMMARY.md`、Trellis 前端规范、React 常见陷阱与实现前检查清单。定位聊天窗口默认尾部加载、顶部继续加载更早消息、聊天记录弹窗定位消息三条链路，确认 `desktop/src/App.tsx` 在历史定位后把目标上下文与当前尾部窗口直接按 message id 合并排序，导致中间缺失消息未被建模却被渲染成连续列表。

## 2. 聊天窗口分页状态修复
修改 `desktop/src/App.tsx` 的消息分页状态，为会话窗口补充 `hasNewer` 与 `loadingNewer` 两个方向状态；默认尾部加载、导入初始化、失败回退都同步初始化双向分页。将历史定位逻辑改为直接切换到目标消息附近的连续上下文窗口，不再把该窗口与原尾部窗口强行合并；同时根据当前已知尾部窗口判断是否还存在向下补齐的后续消息。新增 `handleLoadNewerMessages()`，通过 `after` 游标按升序继续加载定位窗口后方的缺失消息。

## 3. 滚动补齐与回归验证
修改 `desktop/src/frontui/ChatWindow.tsx`，把消息区滚动逻辑从仅支持顶部加载更早消息扩展为同时支持底部加载后续消息，并分别维护顶部/底部 rearm、视口锚点和加载提示文案，避免向下补消息后自动跳到底部。更新 `desktop/tests/frontUiShell.test.tsx` 以匹配新的加载提示；在 `desktop/tests/visualShell.test.tsx` 新增“历史定位后向下滚动补齐中间消息”回归用例。执行 `npm run typecheck` 与 `npm test -- --run desktop/tests/frontUiShell.test.tsx desktop/tests/visualShell.test.tsx`，共 46 项测试通过。

## 4. 头像生图脚本最小实现
阅读 `desktop/public/avatars/image_model.env`、Trellis workflow 与共享 git 规范，确认当前任务只需在头像素材目录下新增最小脚本，不触碰现有前端逻辑。新建 `PRDS/PRD015-头像生图脚本.md`，收敛需求为“读取本地 env、使用 Responses API、支持按行批量 prompt、落盘到独立输出目录并做真实 smoke test”。

## 5. Responses 批量生图脚本与真实请求验证
新增 `desktop/public/avatars/generate_avatars_responses.py` 与 `desktop/public/avatars/avatar_prompts.txt`。脚本使用 `openai` Python SDK 读取 `image_model.env`，通过 `client.responses.create()` + `image_generation` tool 生成图片，解析 `image_generation_call.result` 的 base64 并写入 `generated/` 目录；同时把 `RESPONSE_MODEL_NAME` 和 `IMAGE_MODEL_NAME` 分离，避免把图像模型误填到 Responses 顶层 `model`。执行 `python generate_avatars_responses.py` 前的 SDK smoke test 与原始 HTTP 探测，当前 provider 均返回 `403 / Your request was blocked.`，已确认失败点位于上游网关拦截而非本地脚本语法。

## 6. 错误提示收口
调整 `desktop/public/avatars/generate_avatars_responses.py` 的入口错误处理，将 `OpenAIError` 与本地运行时错误统一收口为简短失败提示，避免后续批量运行时直接输出长 traceback，保留最小脚本结构不变。

## 4. 加载中继续滚动导致视口丢失修复
继续修复 `desktop/src/frontui/ChatWindow.tsx` 的视口冻结问题。确认旧实现只在触发加载的瞬间记录一次锚点；若用户在请求返回前继续滚动，消息插入后仍会按照旧锚点回弹，导致当前正在看的消息丢失。新增加载挂起期间的锚点刷新逻辑：只要 older/newer 分页仍在 pending，每次滚动都会用当前首个可见消息更新冻结锚点，等消息真正插入时再按最新可见位置恢复视口。同步为 `desktop/tests/frontUiShell.test.tsx` 新增“加载更早消息挂起时继续滚动仍保持最新可见位置”回归测试。再次执行 `npm run typecheck` 与定向 `npm test`，共 47 项测试通过。

## 7. 消息窗口模式与历史定位竞态修复
继续治理聊天消息列表定位链路。修改 `desktop/src/lib/chatState.ts`，将历史聊天视图显式拆成 `latest` 尾部窗口与 `anchored` 历史锚点窗口，并提供统一的 latest/anchored 状态构造函数。修改 `desktop/src/App.tsx`，在历史定位时切换到目标上下文窗口，在需要补齐后续消息时保留 anchored 模式；当定位窗口已到最新或向下分页确认没有更多后续消息时恢复 latest 模式。为每个会话维护消息窗口请求版本，历史定位、切会话、导入、退出改写等窗口切换会递增版本，迟到的默认尾部加载结果若版本过期则不会覆盖当前定位窗口。

## 8. 消息列表滚动与回归验证收口
修改 `desktop/src/frontui/ChatWindow.tsx`，新增 `messageWindowMode` 入参，只有 latest 模式才在消息追加、初次加载和切换会话时自动滚到底；anchored 模式只执行一次定位高亮和必要的视口锚点恢复，不会在补页或消息数组变化后重复消费同一个定位请求。同步修复 `conversationKey` 清理 effect 初次挂载后重置已消费定位请求的问题。更新 `desktop/tests/chatState.test.ts`、`desktop/tests/frontUiShell.test.tsx`、`desktop/tests/visualShell.test.tsx`，覆盖 anchored 模式不自动滚底、定位后向下补齐中间消息、迟到默认尾部加载不得覆盖“3月27日 不用补考”锚点窗口。执行 `npm run typecheck` 通过，执行 `npm test` 共 132 项通过。

## 9. 历史定位落点漂移修复
针对 `tests/fixtures/realism_synthetic/case-03-missed-window/conversation.txt` 的真实用例继续排查，确认后端 `readMessageContext()` 返回的目标消息 `不用补考` 及其上下文是正确的，`尽量这个词很你` 只是同一定位窗口内后续第 35 条消息，因此问题不在后端取错消息，而在前端程序化定位后又被其它滚动动作改写。修改 `desktop/src/frontui/ChatWindow.tsx` 的定位流程：历史定位时先短暂抑制分页，再对目标消息执行一次原生滚动预热，随后由消息容器自行计算 `scrollTop` 并按容器中心重复校正，直到目标气泡稳定回到视口中心，避免最终落到同窗口里的后续消息。同步修正 `desktop/tests/visualShell.test.tsx` 中两处历史定位用例的测试喂数与目标选择，并在 `desktop/tests/frontUiShell.test.tsx` 新增“程序化跳转先漂到后续消息，再被重新拉回目标消息中心”的组件级回归。执行 `cd desktop && npm run typecheck`、`cd desktop && npm test -- --run desktop/tests/frontUiShell.test.tsx desktop/tests/visualShell.test.tsx`、`cd desktop && npm test`，共 134 项测试通过。

## 10. 分析失败日志与阶段重试入口
排查完整分析在 `topic_persona_snapshot` 阶段失败后无法重试的问题，确认 `src/if_then_mvp/llm.py` 原先会把所有 HTTP provider 异常统一压成 `Chat completion request failed`，且失败清理会删除 messages、segments、summaries，导致后续无法只重跑失败阶段。修改 `src/if_then_mvp/llm.py`，为 Chat Completions 请求补充非敏感 warning 日志，记录错误类型、HTTP 状态码、尝试次数和是否继续重试，并对 429、502、503、504 与超时执行有限退避重试。修改 `src/if_then_mvp/worker.py` 与 `src/if_then_mvp/conversation_lifecycle.py`，失败时保留已完成上游分析产物；当失败发生在话题整理、人设归纳或关系快照阶段且摘要完整存在时，重试 job 写入 `resume_from_stage=topic_persona_snapshot`，worker 复用已完成摘要，只清理并重跑话题、人设和关系快照阶段。修改 `desktop/src/App.tsx` 与 `desktop/src/frontui/ChatWindow.tsx`，在分析 job 失败时显示“重试失败阶段”入口并恢复轮询状态。更新 `tests/test_llm.py`、`tests/test_worker.py`、`tests/test_conversation_management.py` 和 `desktop/tests/visualShell.test.tsx`，覆盖瞬时 HTTP 状态重试、最终状态码错误暴露、topic 阶段恢复不重跑摘要、后端 retry payload 和前端失败重试入口。

## 11. 摘要阶段部分恢复与禁止伪阶段重试
继续修正阶段重试语义，确认旧失败记录已经删除全部摘要时无法从话题/快照阶段恢复，且新排出的 job 会从片段摘要重新开始。修改 `src/if_then_mvp/conversation_lifecycle.py`，当 failed job 的摘要阶段失败且 segments 存在、summaries 只完成一部分时，重试 job 写入 `resume_from_stage=summarizing`；当 failed job 标记为话题/快照失败但恢复依赖产物已丢失时，直接返回 400 错误，不再悄悄排完整分析。修改 `src/if_then_mvp/worker.py`，新增摘要阶段恢复执行路径：保留已有 `SegmentSummary`，只为缺失 segment 生成摘要，补齐后继续进入 `topic_persona_snapshot` 恢复路径。更新 `tests/test_worker.py` 和 `tests/test_conversation_management.py`，覆盖摘要阶段从缺失摘要继续、话题阶段依赖缺失时报错。执行 `python -m pytest -q` 共 131 项通过，执行 `cd desktop && npm run typecheck` 与 `cd desktop && npm test`，桌面 135 项通过。
