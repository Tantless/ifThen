## 1. 创建 realism-02 brainstorm 任务并核对当前路线图

- 创建 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下面。
- 读取 `AGENTS.md`、`plan/realism-00-roadmap.md`、`plan/TODO.md`、`plan/realism-02-layered-evidence-context.md`、`plan/realism-03-retrieval-ranking-budget.md`、`plan/realism-05-prompt-guardrails.md` 中与真实性路线图相关的内容，确认当前主线下一步应落在 `realism-02`。
- 记录到当前上下文：`plan/TODO.md` 的推荐执行顺序仍保留 `realism-05` 的前置描述，但 `AGENTS.md` 临时路线图和 `plan/realism-00-roadmap.md` 都把 `realism-02` 标成必须先做，后续 brainstorm 以这条主线为准。
- 检查 git 状态与远端同步情况：当前分支 `main` 与 `origin/main` 无 ahead/behind，工作区已有 `.trellis/.template-hashes.json` 和 `.trellis/workspace/tlll1/` 的未提交变更，本次不回滚这些现有改动。

## 2. 建立 realism-02 脑暴 PRD 初稿并补齐代码侧现状

- 新建 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md`，写入目标、已知信息、临时假设、开放问题、需求、验收标准、技术约束和可行方案。
- 记录当前上下文结构：`src/if_then_mvp/retrieval.py` 的 `build_context_pack()` 仍只输出 cutoff-safe 字段；`src/if_then_mvp/branch_sessions.py` 里的 `session_memory_pack` 只包裹 `cutoff_safe_context_pack`；`src/if_then_mvp/simulation.py` 已有“未来证据只能保守使用”的 prompt 文案，但还没有显式分层字段作为输入合同。
- 记录当前代码组织约束：`src/if_then_mvp/api.py` 与 `src/if_then_mvp/worker.py` 各自维护 `_load_related_topic_digests()` 和 context pack 装配逻辑，后续实现若继续扩展未来证据加载，需要有意识控制改动范围并避免双处逻辑漂移。
- 盘点现有测试落点：`tests/test_retrieval.py` 已覆盖 cutoff-safe context pack 基线，`tests/test_branch_sessions.py` 已覆盖 branch session memory pack 和 reply job 主路径，为 `realism-02` 后续补 layered evidence 测试提供现成切入点。

## 3. 锁定 realism-02 的 MVP 边界为方案 B

- 根据用户确认，将 `realism-02` 的实施边界固定为“分层 context contract + realtime memory pack 升级”。
- 将该决定写回 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md` 的 `Decision (ADR-lite)`、`Technical Approach`、`Requirements` 和 `Acceptance Criteria`。
- 明确排除项：本任务不把主要 prompt 行为改写并入同一实现，未来证据的泄漏护栏和回复保守度策略仍归后续 `realism-05` 收口；本任务只允许为兼容现有 prompt 读取路径做必要适配。

## 4. 固定 future evidence 的首版来源范围

- 根据用户确认，将 `future_evidence_digests` 的首版来源固定为 cutoff 后的 `SegmentSummary + Topic/TopicLink`。
- 明确不把 `RelationshipSnapshot` 作为 `realism-02` 首版 future evidence 来源，以控制 loader 复杂度、上下文字段设计复杂度和测试布置范围。
- 将该来源边界同步写回 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md` 的 `Requirements`、`Acceptance Criteria`、`Decision (ADR-lite)` 和 `Technical Approach`。

## 5. 实现分层 context contract 与共享 context builder

- 新增 `src/if_then_mvp/context_builder.py`，统一封装 conversation 级 context pack 装配逻辑，供 `src/if_then_mvp/api.py` 和 `src/if_then_mvp/worker.py` 共用，避免 future evidence loader 在两条路径重复扩散。
- 在 `src/if_then_mvp/context_builder.py` 中新增 `load_future_evidence_digests()`，首版只从 cutoff 后的 `SegmentSummary + Topic/TopicLink` 组装 future evidence，并用 `MAX_FUTURE_EVIDENCE_ITEMS = 3` 控制预算。
- 修改 `src/if_then_mvp/retrieval.py` 的 `build_context_pack()`，新增 `future_evidence_digests` 入参，并输出 `cutoff_safe_facts`、`future_evidence_digests`、`branch_facts`、`evidence_policy`，同时保留 `current_segment_history`、`related_topic_digests`、`same_day_prior_segments` 等旧字段维持兼容。
- 将 `src/if_then_mvp/api.py` 的 `_build_branch_context_pack()` 与 `src/if_then_mvp/worker.py` 的 `_build_simulation_context_pack()` 都改为走共享 `build_conversation_context_pack()`，消除两份手写 context pack 装配逻辑。

## 6. 升级 realtime memory pack 并补齐回归测试

- 修改 `src/if_then_mvp/branch_sessions.py`，把 realtime `session_memory_pack` 从 `realtime-branch-memory-v1` 升级为 `realtime-branch-memory-v2`，显式保存 `layered_context_pack`，并通过 `compatibility.cutoff_safe_context_pack` 保留旧 prompt 读取投影。
- 更新 `tests/test_retrieval.py`，验证 layered context contract 的新字段、future evidence 空集告警和 branch facts 基线结构。
- 更新 `tests/test_branch_sessions.py`，验证 branch session 创建后持久化的 `session_memory_pack` 已包含显式分层结构与兼容投影。
- 更新 `tests/test_simulations.py`，让 simulation 测试基线切到共享 `build_conversation_context_pack()`，并补齐 `build_context_pack()` 新签名所需的 `future_evidence_digests` 参数。
- 运行 `PYTHONPATH=src pytest tests/test_retrieval.py tests/test_branch_sessions.py tests/test_simulations.py -q` 与 `PYTHONPATH=src pytest -q`，当前 104 个测试全部通过。
