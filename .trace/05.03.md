## 1. 创建 realism-02 brainstorm 任务并核对当前路线图

- 创建 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下面。
- 读取 `AGENTS.md`、`plan/realism-00-roadmap.md`、`plan/TODO.md`、`plan/realism-02-layered-evidence-context.md`、`plan/realism-03-retrieval-ranking-budget.md`、`plan/realism-05-prompt-guardrails.md` 中与真实性路线图相关的内容，确认当前主线下一步应落在 `realism-02`。
- 记录到当前上下文：`plan/TODO.md` 的推荐执行顺序仍保留 `realism-05` 的前置描述，但 `AGENTS.md` 临时路线图和 `plan/realism-00-roadmap.md` 都把 `realism-02` 标成必须先做，后续 brainstorm 以这条主线为准。
- 检查 git 状态与远端同步情况：当前分支 `main` 与 `origin/main` 无 ahead/behind，工作区已有 `.trellis/.template-hashes.json` 和 `.trellis/workspace/tlll1/` 的未提交变更，本次不回滚这些现有改动。

## 2. 建立 realism-02 脑暴 PRD 初稿并补齐代码侧现状

- 新建 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md`，写入目标、已知信息、临时假设、开放问题、需求、验收标准、技术约束和可行方案。
- 记录当前上下文结构：`src/if_then_mvp/retrieval.py` 的 `build_context_pack()` 仍只输出 cutoff-safe 字段；`src/if_then_mvp/branch_sessions.py` 里的 `session_memory_pack` 只包裹 `cutoff_safe_context_pack`；`src/if_then_mvp/simulation.py` 已有“未来证据只能保守使用”的 prompt 文案，但还没有显式分层字段作为输入合同。
- 记录当前代码组织约束：`src/if_then_mvp/api.py` 与 `src/if_then_mvp/worker.py` 各自维护 `_load_related_topic_digests()` 和 context pack 装配逻辑，后续实现若继续扩展未来证据加载，需要有意识控制改动范围并避免双处逻辑漂移。
- 盘点现有测试落点：`tests/test_retrieval.py` 已覆盖 cutoff-safe context pack 基线，`tests/test_branch_sessions.py` 已覆盖 branch session memory pack 和 reply job 主路径，为 `realism-02` 后续补 layered evidence 测试提供现成切入点。

## 3. 锁定 realism-02 的 MVP 边界为方案 B

- 根据用户确认，将 `realism-02` 的实施边界固定为“分层 context contract + realtime memory pack 升级”。
- 将该决定写回 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md` 的 `Decision (ADR-lite)`、`Technical Approach`、`Requirements` 和 `Acceptance Criteria`。
- 明确排除项：本任务不把主要 prompt 行为改写并入同一实现，未来证据的泄漏护栏和回复保守度策略仍归后续 `realism-05` 收口；本任务只允许为兼容现有 prompt 读取路径做必要适配。

## 4. 固定 future evidence 的首版来源范围

- 根据用户确认，将 `future_evidence_digests` 的首版来源固定为 cutoff 后的 `SegmentSummary + Topic/TopicLink`。
- 明确不把 `RelationshipSnapshot` 作为 `realism-02` 首版 future evidence 来源，以控制 loader 复杂度、上下文字段设计复杂度和测试布置范围。
- 将该来源边界同步写回 `.trellis/tasks/05-03-05-03-realism-02-layered-evidence-context/prd.md` 的 `Requirements`、`Acceptance Criteria`、`Decision (ADR-lite)` 和 `Technical Approach`。

## 5. 实现分层 context contract 与共享 context builder

- 新增 `src/if_then_mvp/context_builder.py`，统一封装 conversation 级 context pack 装配逻辑，供 `src/if_then_mvp/api.py` 和 `src/if_then_mvp/worker.py` 共用，避免 future evidence loader 在两条路径重复扩散。
- 在 `src/if_then_mvp/context_builder.py` 中新增 `load_future_evidence_digests()`，首版只从 cutoff 后的 `SegmentSummary + Topic/TopicLink` 组装 future evidence，并用 `MAX_FUTURE_EVIDENCE_ITEMS = 3` 控制预算。
- 修改 `src/if_then_mvp/retrieval.py` 的 `build_context_pack()`，新增 `future_evidence_digests` 入参，并输出 `cutoff_safe_facts`、`future_evidence_digests`、`branch_facts`、`evidence_policy`，同时保留 `current_segment_history`、`related_topic_digests`、`same_day_prior_segments` 等旧字段维持兼容。
- 将 `src/if_then_mvp/api.py` 的 `_build_branch_context_pack()` 与 `src/if_then_mvp/worker.py` 的 `_build_simulation_context_pack()` 都改为走共享 `build_conversation_context_pack()`，消除两份手写 context pack 装配逻辑。

## 6. 升级 realtime memory pack 并补齐回归测试

- 修改 `src/if_then_mvp/branch_sessions.py`，把 realtime `session_memory_pack` 从 `realtime-branch-memory-v1` 升级为 `realtime-branch-memory-v2`，显式保存 `layered_context_pack`，并通过 `compatibility.cutoff_safe_context_pack` 保留旧 prompt 读取投影。
- 更新 `tests/test_retrieval.py`，验证 layered context contract 的新字段、future evidence 空集告警和 branch facts 基线结构。
- 更新 `tests/test_branch_sessions.py`，验证 branch session 创建后持久化的 `session_memory_pack` 已包含显式分层结构与兼容投影。
- 更新 `tests/test_simulations.py`，让 simulation 测试基线切到共享 `build_conversation_context_pack()`，并补齐 `build_context_pack()` 新签名所需的 `future_evidence_digests` 参数。
- 运行 `PYTHONPATH=src pytest tests/test_retrieval.py tests/test_branch_sessions.py tests/test_simulations.py -q` 与 `PYTHONPATH=src pytest -q`，当前 104 个测试全部通过。

## 7. 修复 realism-02 memory pack review 语义问题

- 修改 `src/if_then_mvp/branch_sessions.py`，将 `compatibility.cutoff_safe_context_pack` 改为只保留 cutoff-safe 白名单字段的兼容投影，不再把整份 `context_pack` 原样写入，从而阻断 `future_evidence_digests` 通过“角色可知”包装重新暴露。
- 在 `src/if_then_mvp/branch_sessions.py` 中新增 branch-session memory pack 刷新逻辑，并在创建 session、追加 self 消息后重建 `layered_context_pack.branch_facts.generated_branch_messages`，使持久化 branch facts 与当前 committed 分支消息保持一致。
- 修改 `src/if_then_mvp/worker.py`，在写入新的 `other` 分支回复并更新 `current_branch_state` 后，同步刷新 `session_memory_pack` 中的 branch facts，避免第二轮开始 prompt 同时看到新 transcript 和陈旧 branch facts。
- 更新 `tests/test_branch_sessions.py`，补充兼容投影不得包含 `future_evidence_digests` / `branch_facts` / `evidence_policy` 的断言，并验证首轮、第二轮 reply 完成后持久化 `generated_branch_messages` 会准确反映已提交的分支 transcript。
- 运行 `PYTHONPATH=src pytest tests/test_branch_sessions.py -q`、`PYTHONPATH=src pytest tests/test_retrieval.py tests/test_simulations.py -q` 和 `PYTHONPATH=src pytest -q`，当前 104 个测试全部通过。

## 8. 创建 realism-03 检索排序与预算任务并收敛首版范围

- 创建 `.trellis/tasks/05-03-realism-03-retrieval-ranking-budget/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，作为 `realism-02` 之后的下一执行步骤。
- 新建 `.trellis/tasks/05-03-realism-03-retrieval-ranking-budget/prd.md`，把首版范围收敛为“共享 context builder 上的确定性排序 + 上下文预算 + retrieval trace”，明确不引入 embedding、不改 prompt 文案、不增加新的分析产物。
- 新建 `PRDS/PRD006-检索排序预算.md`，记录已确认的终版 PRD，绑定本轮 `realism-03` 任务的目标、需求和验收标准。
- 读取并整理 `.trellis/spec/shared/code-quality.md`、`.trellis/spec/shared/typescript.md`、`.trellis/spec/shared/git-conventions.md`、`.trellis/spec/guides/cross-layer-thinking-guide.md`、`.trellis/spec/guides/code-reuse-thinking-guide.md` 中与本任务相关的约束，确认检索逻辑应集中在 `src/if_then_mvp/context_builder.py`，context pack 打包仍由 `src/if_then_mvp/retrieval.py` 负责。

## 9. 实现 realism-03 检索排序、预算控制与回归测试

- 修改 `src/if_then_mvp/context_builder.py`，为 `related_topic_digests` 和 `future_evidence_digests` 建立统一候选结构，按目标段 topic 重合、敏感度、稳定性与时间接近度进行确定性排序，并在共享 builder 内执行预算裁剪，避免 API、worker、realtime branch 三条路径各自产生不同检索结果。
- 修改 `src/if_then_mvp/retrieval.py`，让 `build_context_pack()` 输出 `retrieval_trace` 和 `retrieval_budget`，显式记录每类证据的预算、候选数、选中数与命中原因，同时保留现有 layered evidence 字段合同，避免影响现有 prompt 消费路径。
- 更新 `tests/test_retrieval.py`，补充默认预算元数据和显式 trace/budget 透传断言，验证 context pack 在无 topic/future evidence 时仍返回稳定的检索元信息。
- 更新 `tests/test_simulations.py`，通过真实 DB 装配路径验证“同 topic 且敏感的 future 边界证据优先于无关未来闲聊”和“没有 topic link 时仍保留 `same_day_prior_segments` 与 snapshot 退化路径”两类 `realism-03` 关键回归。
- 运行 `PYTHONPATH=src pytest tests/test_retrieval.py -q`、`PYTHONPATH=src pytest tests/test_simulations.py -q`、`PYTHONPATH=src pytest tests/test_retrieval.py tests/test_simulations.py -q` 和 `PYTHONPATH=src pytest -q`，当前 107 个测试全部通过。

## 10. 创建 realism-04 人格与表达风格增强任务并绑定 PRD

- 创建 `.trellis/tasks/05-03-05-03-realism-04-persona-style-enrichment/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，作为 `realism-03` 之后的下一步实现。
- 新建 `.trellis/tasks/05-03-05-03-realism-04-persona-style-enrichment/prd.md`，将首版范围收敛为“cutoff-safe deterministic style profile + generation prompt 风格护栏”，明确不新增 LLM 分析、不新增存储表、不改外部 API。
- 新建 `PRDS/PRD007-人格风格增强.md`，把本轮终版 PRD 绑定到同一文档，后续将在该文件下回填工作记录及完成情况。
- 读取并整理 `src/if_then_mvp/context_builder.py`、`src/if_then_mvp/simulation.py`、`src/if_then_mvp/branch_sessions.py`、`desktop/src/lib/frontUiAdapters.ts` 与 shared / guide 规范，确认 deterministic style profile 应集中在共享 context builder 内实现，并通过现有 `persona_self` / `persona_other` 路径透传给 simulation 与 realtime branch。

## 11. 实现 realism-04 deterministic style profile 与 prompt 风格护栏

- 修改 `src/if_then_mvp/context_builder.py`，为 cutoff 前消息新增 deterministic style profile 统计逻辑：按角色分别计算全局风格、近期互动风格、连续短句/多泡习惯、常见语气词/标点、问句倾向与压力下表达强度边界，并把结果以 `deterministic_style_profile` 合并到 `persona_self` / `persona_other`。
- 修改 `src/if_then_mvp/simulation.py`，让 first reply、next turn 和 realtime branch reply prompt 显式消费 `deterministic_style_profile.reply_envelope` / `generation_hints`，把长度上限、单泡/双短泡倾向与压力下保留边界写成生成护栏，同时保持 branch assessment prompt 合同不变。
- 更新 `tests/test_simulations.py`，补充 deterministic style profile 计算回归，并验证首轮回复、多轮续写 prompt 已包含 style profile、`max_bubble_count` 和相应护栏；更新 `tests/test_branch_sessions.py`，验证 realtime branch reply prompt 也会透传 persona_other 风格护栏。
- 运行 `PYTHONPATH=src pytest tests/test_retrieval.py tests/test_simulations.py tests/test_branch_sessions.py -q` 与 `PYTHONPATH=src pytest -q`，当前 108 项测试全部通过。

## 12. 创建 realism-05 提示词护栏任务并绑定 PRD

- 创建 `.trellis/tasks/05-03-realism-05-prompt-guardrails/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，作为 `realism-04` 之后的下一步实现。
- 新建 `.trellis/tasks/05-03-realism-05-prompt-guardrails/prd.md`，将本轮范围收敛为“branch assessment、first reply、next turn 与 realtime branch reply 的 future evidence 泄漏护栏”，明确不新增外部 API、不新增存储表、不新增 LLM 分析阶段。
- 新建 `PRDS/PRD008-提示词护栏.md`，把本轮终版 PRD 绑定到同一文档，后续将在该文件下回填工作记录及完成情况。
- 读取 `plan/realism-05-prompt-guardrails.md`、`plan/realism-00-roadmap.md`、`plan/TODO.md` 与 shared/backend/guides 规范，确认本轮改动应集中在 `src/if_then_mvp/simulation.py` 的既有 prompt builder 与相关测试中。

## 13. 实现 realism-05 future evidence 泄漏护栏并完成验证

- 修改 `src/if_then_mvp/simulation.py`，为 `BranchAssessmentPayload` 新增 `modeler_only_risk_sources` 与 `leakage_boundary_notes`，并在 branch assessment prompt 中明确 character-known、modeler-only 和 branch-only 三类证据边界。
- 修改 `src/if_then_mvp/simulation.py`，在 first reply、next turn 与 realtime branch reply prompt 中加入 future evidence 泄漏禁止规则，要求 cutoff 后证据只能影响表达保守程度、风险与置信度，不能进入角色台词或被搬进当前分支事实。
- 更新 `tests/test_simulations.py`，补充 future evidence 中出现明确拒绝/偏好边界时的 prompt 护栏回归，并验证旧 `single_reply` 入口仍保持兼容的 prompt 结构。
- 更新 `tests/test_branch_sessions.py`，通过真实 branch session/worker 路径构造 cutoff 后 evidence，验证 realtime other reply prompt 将该 evidence 标为 modeler-only 并禁止强行搬入分支。
- 运行 `PYTHONPATH=src pytest tests/test_simulations.py tests/test_branch_sessions.py -q`、`PYTHONPATH=src pytest tests/test_worker.py -q` 和 `PYTHONPATH=src pytest -q`，当前 110 项测试全部通过。

## 14. 创建 realism-08 质量验收与 rollout 任务

- 创建 `.trellis/tasks/05-03-realism-08-quality-rollout/` 任务目录，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，作为真实性路线图收口任务。
- 新建 `.trellis/tasks/05-03-realism-08-quality-rollout/prd.md`，将本轮范围收敛为“后端 quality gate 测试、future evidence feature flag、rollout 文档和计划矩阵更新”，明确不新增前端 E2E、不新增真实 LLM 批量评测链路、不修改外部 API 或数据库 schema。
- 新建 `PRDS/PRD009-质量验收.md`，把本轮终版 PRD 绑定到同一文档，后续将在该文件下回填工作记录及完成情况。
- 读取 `plan/realism-08-quality-and-rollout.md`、`plan/realism-00-roadmap.md`、`plan/TODO.md` 与 shared/guides 规范，确认本轮实现要集中覆盖已完成真实性能力的可回归合同，而不是扩展新的产品行为。

## 15. 实现 realism-08 quality gate、future evidence 开关与 rollout 文档

- 修改 `src/if_then_mvp/config.py`，新增 `IF_THEN_ENABLE_FUTURE_EVIDENCE` 布尔配置，默认启用，并支持用 `0`、`false`、`off`、`no` 关闭 future evidence。
- 修改 `src/if_then_mvp/context_builder.py`，在共享 context builder 内接入 future evidence 开关；关闭时返回空 `future_evidence_digests`、空 trace、稳定 budget 和 `future_evidence_disabled` warning，同时保留 cutoff-safe related topic digest。
- 新增 `tests/test_realism_quality_gate.py`，集中覆盖 context pack 分层合同、future evidence 与 cutoff-safe 隔离、assessment 风险字段、first reply 泄漏边界、realtime branch 串行 job、branch transcript 顺序、删除会话清理和固定 baseline 泄漏标注合同。
- 新增 `docs/realism-quality-rollout.md`，记录 future evidence 回滚开关、必跑测试命令、桌面测试命令、质量门禁、人工验收标准、rollout 步骤和观测指标。
- 更新 `plan/realism-08-quality-and-rollout.md`、`plan/TODO.md` 和 `plan/realism-00-roadmap.md`，同步已完成的后端门禁、评估样例接入、feature flag 和 rollout 文档状态，并保留前端自动化 E2E 与真实 provider 回归的未完成边界。
- 运行 `PYTHONPATH=src pytest tests/test_realism_quality_gate.py -q`、相关后端回归和 `PYTHONPATH=src pytest -q`，当前 114 项测试全部通过。

## 16. 对齐 realism 任务元数据状态

- 核对 `.trellis/tasks/05-03-05-03-realism-02-memory-pack-review-fixes/task.json` 与提交 `aac01b7`，确认 `realism-02` memory pack review fixes 已完成实现、验证和提交记录，将任务状态从 `in_progress` 修正为 `completed`，补齐 `completedAt`、`current_phase`、`next_action` 和 `commit` 字段。
- 核对 `.trellis/tasks/05-03-realism-03-retrieval-ranking-budget/task.json` 与提交 `51c04cf`，确认 `realism-03` 检索排序与预算已完成实现、验证和提交记录，将任务状态从 `in_progress` 修正为 `completed`，补齐 `completedAt`、`current_phase`、`next_action` 和 `commit` 字段。

## 17. 创建 realism-08 前端质量验收收尾任务

- 创建 `.trellis/tasks/05-03-realism-08-frontend-quality-closure/` 子任务，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，用于收口路线图中仍未勾选的前端自动化验收项。
- 新建 `.trellis/tasks/05-03-realism-08-frontend-quality-closure/prd.md`，将任务范围限定为补充桌面端自动化测试与更新 rollout 文档，不修改实时分支生产行为。
- 新建 `PRDS/PRD010-前端质量.md`，记录本轮前端质量验收收尾的终版 PRD 和验收标准。

## 18. 补齐 realism-08 前端自动化验收

- 修改 `desktop/tests/visualShell.test.tsx`，新增 realtime branch running job 期间追加 self 消息的集成测试，验证前端进入合并状态，并且在追加消息提交与 1.8 秒 idle window 结束前不会并行触发新的 branch reply job。
- 修改 `desktop/tests/visualShell.test.tsx`，新增 branch reply job 失败后的错误展示和重试测试，验证失败原因显示、重试按钮可用，并且重试仍遵守 idle window 后创建 reply job 的节流规则。
- 更新 `plan/realism-08-quality-and-rollout.md`、`docs/realism-quality-rollout.md`、`plan/realism-00-roadmap.md`、`plan/TODO.md` 和 `AGENTS.md`，同步前端自动化验收完成状态，并将后续路线图剩余项收敛为 provider/人工验收与完整 pipeline 性能复测。
- 更新 `PRDS/PRD010-前端质量.md`，写入本次任务的工作记录及完成情况。
- 在安装 `desktop` 依赖后运行 `npm test -- desktop/tests/visualShell.test.tsx`，当前 22 项桌面集成测试全部通过。

## 19. 修复桌面完整测试中的 Vite 构建超时阈值

- 复跑 `npm test` 时确认新增 `visualShell` 测试通过，但 `desktop/tests/frontUiShell.test.tsx` 中两个 Vite build 验证依赖默认 5 秒测试超时，当前本地构建约 5.2 秒，导致完整测试稳定失败。
- 修改 `desktop/tests/frontUiShell.test.tsx`，只为两个执行真实 Vite build 的测试设置 15 秒显式超时，保留原有断言和生产代码不变。

## 20. 完成 realism-08 前端质量收口验证

- 运行 `npm test`，桌面端 13 个测试文件共 128 项测试全部通过，覆盖新增 realtime branch 前端质量回归和既有 frontUI 构建验证。
- 运行 `npm run typecheck`，`tsconfig.json`、`tsconfig.node.json` 和 `tsconfig.test.json` TypeScript 检查全部通过。
- 更新 `.trellis/tasks/05-03-realism-08-frontend-quality-closure/task.json`，将任务状态标记为 `completed`，补齐完成日期、阶段、相关文件和验证说明。

## 21. 记录 realism 路线图剩余验收状态

- 创建 `.trellis/tasks/05-03-realism-roadmap-validation-status/` 子任务，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，用于记录本地门禁通过后的路线图剩余验收边界。
- 新建 `.trellis/tasks/05-03-realism-roadmap-validation-status/prd.md` 和 `PRDS/PRD011-路线验收.md`，明确本任务只更新路线图验证状态，不修改业务逻辑。
- 运行 `PYTHONPATH=src pytest -q`，当前 114 项后端测试全部通过。
- 运行 `python scripts/report_realism_baseline.py`，确认 12 个固定样例覆盖 3 个场景和 8 类失败类型。
- 更新 `plan/realism-00-roadmap.md`、`plan/realism-08-quality-and-rollout.md` 和 `docs/realism-quality-rollout.md`，记录本地自动化验收已通过，并保留真实 provider 回归、人工拟真性验收和 5000 条完整分析性能复测的未完成状态。

## 22. 创建 realism provider 回归运行器任务

- 创建 `.trellis/tasks/05-03-realism-provider-regression-runner/` 子任务，并将其挂到父任务 `04-26-brainstorm-todo-performance-realism` 下，用于推进路线图中仍未完成的真实 provider 输出验收。
- 新建 `.trellis/tasks/05-03-realism-provider-regression-runner/prd.md`，将任务范围限定为固定基线样例的 provider 回归命令、fake provider 测试和文档入口，不修改生产实时分支会话逻辑。
- 新建 `PRDS/PRD012-Provider回归.md`，记录本轮 provider 回归运行器的终版 PRD、需求和验收标准。

## 23. 实现 provider 回归运行器与测试

- 新增 `scripts/run_realism_provider_regression.py`，从 `tests/fixtures/realism_baseline/cases.json` 读取固定样例，为每个样例构造 prompt-level context pack，并复用 `assess_branch()` 与 `generate_first_reply()` 执行真实 provider 或 replay 输出回归。
- 在 `scripts/run_realism_provider_regression.py` 中实现 `first_reply_text` 的 future leakage 检测，使用每个样例的 `modeler_only_evidence.forbidden_character_knowledge` 作为 forbidden term 来源，并在 JSON summary 中统计完成、跳过、错误、泄漏和人工风险复核数量。
- 在 `scripts/run_realism_provider_regression.py` 中实现缺少 provider 配置时的 skipped 报告，保留 `--require-provider` 供阻塞式验收使用，并支持 `--replay-output` 在不调用真实 LLM 的情况下复现报告结构。
- 新增 `tests/test_realism_provider_regression.py`，覆盖 forbidden term 泄漏检测、fake provider 走生产 prompt builder、缺配置 skipped 报告和 replay CLI 生成 JSON/Markdown 报告。
- 更新 `PRDS/PRD012-Provider回归.md`、`plan/realism-00-roadmap.md`、`plan/realism-08-quality-and-rollout.md`、`plan/TODO.md` 和 `docs/realism-quality-rollout.md`，把路线图剩余 provider/人工验收入口指向新增命令，并明确真实通过仍需本地 LLM 配置。

## 24. 验证 provider 回归运行器并完成任务状态

- 运行 `PYTHONPATH=src pytest tests/test_realism_provider_regression.py tests/test_realism_baseline_fixtures.py -q`，新增 provider 回归测试与 baseline fixture 测试共 8 项通过。
- 运行 `python scripts/report_realism_baseline.py`，确认固定 baseline 报告仍输出 12 个样例、3 个场景和 8 类失败类型覆盖矩阵。
- 运行 `python scripts/run_realism_provider_regression.py --max-cases 1 --output-json .pytest-tmp/realism-provider-skip.json --output-markdown .pytest-tmp/realism-provider-skip.md`，在缺少 `local_llm_config.py` 的当前 workspace 中生成 skipped JSON/Markdown 报告。
- 运行 `PYTHONPATH=src pytest -q`，当前后端测试 118 项全部通过。
- 更新 `.trellis/tasks/05-03-realism-provider-regression-runner/task.json`，将任务状态标记为 `completed`，补齐完成日期、阶段、相关文件和验证说明。
