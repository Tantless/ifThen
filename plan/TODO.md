# TODO
项目所有待办项

## 更新时间
2026年4月11日13点37分

## 高工作量 (L1)

### Windows 打包发布
配置 Electron 打包、Python 运行时随包分发、数据目录策略、日志系统、发布测试。
- 预计工作量：高

### 重构项目代码
- 整合 docs 中的各种开发文档，规范化目前的开发进度，归结到同一个文档中，标好时间线，用概括性的语言概括项目历史进度。
- 整合好 docs 文档中，则清除原本的历史文档
- 去除项目中的所有临时文件，即当前项目没用到，github 仓库大概也不需要的内容
- 规范化项目结构，让项目文件不再看起来杂乱
- **此阶段需要频繁与我沟通，确认重构颗粒度和边界**
- 预计工作量：高

## 中工作量 (L2)

### 优化前端推演时的小字进度条展示（已完成）
将轮转的小字提示轮转这种虚假的进度，替换成真实的推演流程提示。
- 完成时间：2026年4月11日
- 已改为根据推演模式展示真实流程说明，不再轮播伪阶段文案。
- 预计工作量：中

### 开放替换头像功能（已完成）
- 在进入前端页面第一次初始化时，支持选取自己的头像，需要支持默认5个头像，也要支持用户自己利用本地文件上传头像。
- 导入聊天记录时，支持设置对方头像，同样支持默认5个头像，也可以支持用户利用本地文件上传。
- 需要用到下载功能，去下载一些主流的头像
- 完成时间：2026年4月12日
- 已提供 5 个内置头像预设，并支持通过桌面端选择本地图片作为头像。
- `self` 头像持久化到 `profile.self_avatar_url`，首次欢迎流程和设置页都可以修改。
- 导入聊天记录时可为对方选择头像，持久化到 `conversation.{id}.other_avatar_url`，聊天列表和消息区会直接使用。
- 预计工作量：中

### 分离分析使用模型和推演使用模型（已完成）
在设置页面，可以分别设置分析使用模型和推演使用模型。
- 完成时间：2026年4月11日
- 已实现前后端分离配置：后端 runtime_llm.py 支持 role 参数区分 API 和 worker 角色，前端 SettingsDrawer 添加独立的 Simulation Model 输入框。
- llm.simulation_model 为空时自动回退到 llm.chat_model。
- 预计工作量：低

### 将页面右侧的展示段落、话题、主题的附栏去除（已完成）
转而变成一个弹窗查看的形式。
- 整体页面更加还原微信，原本的附栏更像是原微信的一个插件功能。
- 完成时间：2026年4月12日
- 已将 `AnalysisInspector` 从主界面右侧附栏改为独立弹窗，通过聊天头部“分析”按钮打开。
- 弹窗内保留话题 / 人格 / 快照三标签切换，并补充前端回归测试锁定 modal 展示。
- 预计工作量：中

### 拆分导入聊天记录和分析（已完成）
聊天记录导入之后，先展示导入到我们聊天列表和消息页面的效果，再由用户自行点击按钮继续进行接下来的分析（分段，分话题，分主题，解析人格快照等）费时操作。
- 完成时间：2026年4月12日
- 已支持导入时通过 `auto_analyze` 选择仅完成消息导入，导入后会停留在 `imported` 状态。
- 聊天窗口会在未分析会话上展示“开始分析”入口，手动启动后再排入完整分析队列。
- 已补充后端与前端回归测试，锁定 `import_only`、`/conversations/{id}/start-analysis` 和界面状态切换。
- 预计工作量：中

### Prompt 工程优化（已完成）
优化 Persona、Snapshot、Branch Assessment、First Reply、Next Turn 的 prompt。
- 完成时间：2026年4月12日
- 已强化五组 prompt 的保守判断、真实聊天措辞、自然收束约束，并补充测试锁定这些约束。
- 预计工作量：中

## 低工作量 (L3)

### 清理开发环境数据库脏数据（已完成）
将未导入成功的数据清理。
- 完成时间：2026年4月11日
- 已创建清理脚本 scripts/clean_failed_conversations.py，成功清理 9 个失败会话及其相关数据。
- 预计工作量：低

### 联系人和文件 Tab（已完成）
实现联系人列表和文件管理功能。
- 完成时间：2026年4月11日
- 已实现联系人 Tab，从会话数据中提取联系人列表，支持点击跳转到对应会话。
- 文件 Tab 使用 mock 数据占位，等待后端 API 支持。
- 预计工作量：低

### 将 /simulations 切换为队列任务（已完成）
- 完成时间：2026年4月12日
- `POST /simulations` 现在返回 queued 的 `SimulationJob`，worker 会异步产出最终 `Simulation` 结果。
- 已补充 `simulation-jobs` 列表、`GET /simulations/{id}` 结果读取、CLI 输出和桌面端 pending/完成态回归测试。
- 预计工作量：低
