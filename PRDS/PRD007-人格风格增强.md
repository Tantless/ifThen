## prd-人格风格增强-已确认

### Goal

在不新增 LLM 分析调用和不显著增加完整分析耗时的前提下，为 shared context pack 增补 deterministic style profile，让 simulation / realtime branch 的回复长度、拆泡倾向、语气词/标点和压力下表达强度更像真实对方。

### Requirements

- 基于 cutoff-safe 原消息确定性计算 `self` / `other` 的 style profile，不调用新的 LLM 分析。
- 区分全局风格与当前关系中的近期互动风格，并把结果并入 `persona_self` / `persona_other` 的 persona/style 区域。
- 输出 `reply_envelope` / `generation_hints`，至少覆盖长度上限、单泡/多泡倾向、常见语气词、常见标点、压力下表达强度边界。
- prompt 显式要求生成侧优先遵守 `persona_other` 的 deterministic style profile，`persona_self` 仅用于理解。
- 保持现有 simulation / realtime branch / session memory pack 消费路径兼容，不新增存储表，不修改外部 API 合同。

### Acceptance Criteria

- `context_pack["persona_self"]` / `context_pack["persona_other"]` 包含 deterministic style profile，且只使用 cutoff-safe 消息计算。
- style profile 能区分全局风格与近期互动风格，并提供可直接消费的 `reply_envelope` / `generation_hints`。
- 首轮回复、短链续写和 realtime branch reply prompt 都能拿到 other 的 style 约束。
- 偏短句角色不会在 prompt 里被放任生成长篇分析式回复，也不会被鼓励无限拆泡。
- `PYTHONPATH=src pytest tests/test_simulations.py tests/test_branch_sessions.py -q` 与 `PYTHONPATH=src pytest -q` 通过。

## 工作记录及完成情况

- 已修改 `src/if_then_mvp/context_builder.py`，在 shared context builder 内新增 cutoff-safe deterministic style profile 计算，按 `self` / `other` 分别统计全局风格、近期互动风格、连续短句/多泡习惯、常见语气词/标点与压力下表达强度边界，并通过 `deterministic_style_profile` 合并进 `persona_self` / `persona_other`。
- 已修改 `src/if_then_mvp/simulation.py`，让首轮回复、多轮续写和 realtime branch reply prompt 显式读取 `persona_other` / 当前说话者的 `deterministic_style_profile`，把 `reply_envelope` 与 `generation_hints` 作为长度、拆泡和保守度护栏写入 prompt；branch assessment prompt 保持原有合同不变。
- 已更新 `tests/test_simulations.py`，增加 context builder 风格统计回归，并验证 first reply / next turn prompt 已包含 deterministic style profile、`max_bubble_count` 和风格护栏；同时更新 `tests/test_branch_sessions.py`，验证 realtime branch reply prompt 也能拿到 persona_other 风格约束。
- 已运行 `PYTHONPATH=src pytest tests/test_retrieval.py tests/test_simulations.py tests/test_branch_sessions.py -q` 与 `PYTHONPATH=src pytest -q`，当前 108 项测试全部通过。
