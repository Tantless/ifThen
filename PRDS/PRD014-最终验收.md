# PRD014-最终验收

## prd-最终验收-完成情况

### 背景

真实性路线图的实现开发已经完成，但 `plan` 中仍剩两类非人工代码任务没有收口：

- 真实 provider 固定样例回归不能只停留在 1 个 smoke case；
- 基于 `tests/fixtures/realism_synthetic` 的完整分析性能抽样还没有可复用命令和最新报告。

### 目标

补齐最终非人工验收闭环，让剩余未完成项只保留给人工拟真性签收。

### 需求

- 复用现有 provider 回归命令执行更完整的真实样例回归。
- 新增可复用的 synthetic 完整分析性能抽样命令，输出 JSON/Markdown 报告。
- 使用隔离数据目录运行性能抽样，不污染默认 `.data`。
- 更新路线图、rollout、TODO、trace，明确只剩人工验收。

### 验收标准

- [x] provider 回归报告成功生成，并记录泄漏/错误/需人工复核样例数。
- [x] synthetic 完整分析性能 runner 已生成报告，并记录 live full-analysis 阻塞点。
- [x] 文档状态更新完成。
- [ ] 完成 git commit。

## 工作记录及完成情况

- 已执行 `python scripts/run_realism_provider_regression.py --require-provider --output-json .pytest-tmp/realism-provider-live-full.json --output-markdown .pytest-tmp/realism-provider-live-full.md`，12 个固定 baseline case 中 11 个 completed、0 泄漏、0 risk-review flag、1 个瞬时 `HTTPStatusError`。
- 已对失败样例 `c03-rp1-walk-invite` 单独重跑 `--case-id`，该样例通过，说明当前剩余边界是 provider 稳定性而不是确定性泄漏回归。
- 已新增 `scripts/run_realism_analysis_performance.py` 与 `tests/test_realism_analysis_performance.py`，把 committed synthetic corpus 的完整分析性能抽样收敛成可重复命令。
- 已执行 synthetic full-analysis live 抽样，确认 runner 能驱动真实 `run_next_job()` 全分析链路、生成隔离 data dir、worker log 和 JSON/Markdown 报告。
- 已确认当前 long-run performance 验收阻塞在 live provider 通道稳定性：摘要阶段出现 `HTTPStatusError` / `ReadTimeout`，尚未拿到完整成功样本，因此不能勾选“真实性改造没有显著增加完整 pipeline 耗时”。
- 已更新 `plan/realism-00-roadmap.md`、`plan/realism-08-quality-and-rollout.md`、`plan/TODO.md`、`docs/realism-quality-rollout.md` 和 `docs/analysis-performance-realism-synthetic-20260504.md`，把剩余边界收敛为人工拟真性签收与稳定 provider 下的 live full-analysis 性能复测。
