## prd-质量验收-已确认

### Goal

为真实性路线图建立首版可执行质量门禁和 rollout 记录：用后端回归测试锁住分层证据、future evidence 泄漏、realtime branch 串行与旧入口兼容，并提供可关闭 future evidence 的 feature flag 和从短链入口切到实时分支入口的 rollout 文档。

### Requirements

- 建立后端 realism quality gate 测试，集中覆盖 context pack 分层合同、future evidence 与 cutoff-safe 隔离、assessment 风险字段、first reply 泄漏边界、realtime branch 串行 job、branch transcript 顺序和删除会话清理。
- 将固定拟真性 baseline 样例纳入质量门禁，至少验证 baseline 样例数量、失败类型覆盖和 future leakage 标注合同。
- 增加 `IF_THEN_ENABLE_FUTURE_EVIDENCE` 环境开关，默认启用；关闭时 context pack 不加载 future evidence，并给出明确 retrieval warning / budget 元数据。
- 增加 rollout 文档，记录短链兼容入口、实时分支主入口、future evidence 开关、必跑测试命令、人工验收标准和观测指标。
- 更新 `plan/realism-08-quality-and-rollout.md` 的自动测试矩阵和 rollout checklist，使路线图状态与实际回归测试一致。
- 保持外部 API、数据库 schema 与前端实现不变；前端自动化 E2E 不纳入本轮。

### Acceptance Criteria

- 新增后端 quality gate 测试，并能作为 `PYTHONPATH=src pytest tests/test_realism_quality_gate.py -q` 单独运行。
- 关闭 `IF_THEN_ENABLE_FUTURE_EVIDENCE` 后，`future_evidence_digests` 与相关 trace 为空，并保留 cutoff-safe topic digest 与旧入口兼容。
- 固定 baseline 样例被 quality gate 引用，未来泄漏样例合同仍可检查。
- Rollout 文档明确上线步骤、回滚开关、必跑测试、人工验收标准和观测指标。
- `PYTHONPATH=src pytest -q` 通过。

## 工作记录及完成情况

- 已修改 `src/if_then_mvp/config.py`，新增 `IF_THEN_ENABLE_FUTURE_EVIDENCE` 布尔配置，默认启用，支持用 `0`、`false`、`off`、`no` 关闭 future evidence。
- 已修改 `src/if_then_mvp/context_builder.py`，在共享 context builder 内接入 future evidence 开关；关闭时不加载 cutoff 后证据，保留空 `future_evidence_digests`、空 trace、稳定 budget，并追加 `future_evidence_disabled` warning，同时不影响 cutoff-safe related topic digest。
- 已新增 `tests/test_realism_quality_gate.py`，集中覆盖分层 context 合同、future evidence feature flag、assessment/modeler-only prompt 泄漏边界、realtime branch job supersede、branch transcript 顺序、删除会话清理和固定 baseline future leakage 标注合同。
- 已新增 `docs/realism-quality-rollout.md`，记录 future evidence 回滚开关、必跑 pytest 命令、桌面测试命令、质量门禁、人工验收标准、rollout 步骤和观测指标。
- 已更新 `plan/realism-08-quality-and-rollout.md`、`plan/TODO.md` 与 `plan/realism-00-roadmap.md`，同步当前真实性路线图完成状态和仍需人工/前端自动化补齐的边界。
- 已运行 `PYTHONPATH=src pytest tests/test_realism_quality_gate.py -q`、相关后端回归和 `PYTHONPATH=src pytest -q`，当前 114 项测试全部通过。
